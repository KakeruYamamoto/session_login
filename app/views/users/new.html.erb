<h1>Sign up</h1>

<%= form_with(model: @user, local: true) do |f| %> <%# #view/newのform_withを使用する場合createアクションのルーティングが必要になる %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation %>

  <%= f.submit "Create my account" %>
<% end %>
